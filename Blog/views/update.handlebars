<section>
    <h1>{{comment.user.name}}</h1>
    <h1>{{comment.commentPost}}</h1>
    {{debug comment}}
    </section>
    <div class="blog-comment" id="comment-form">
        <label for="comment-input">Update Comment:</label><br>
        <textarea class="input" style="width: 75%; height: 50px" id="comment-text">{{comment.commentPost}}</textarea><br>
        <button id="comment-submit" type="submit" class="comment-card-submit">Submit Update</button>
    </div>
</section>
